# The layered architecture pattern: Router → Service → Repository


## Router (or "API" layer) — handles HTTP concerns only

## Service layer — contains your actual business logic

## Repository layer — the only layer that actually talks to the database.
